

# Update Design System Documentation

## What Changes

Replace the existing MDX documentation files with the newly uploaded versions. The content is substantively identical to what's already saved, with the same design principles (semantic colors, Inter font, CSS variables, pre-built components). The files will be overwritten with the latest versions to keep documentation current.

## File Changes

### 1. `src/docs/design-system-instructions.mdx`
Overwrite with contents of `design-system-instructions-2.mdx`.

### 2. `src/docs/design-system-quick-reference.mdx`
Overwrite with contents of `design-system-quick-reference-2.mdx`.

No other files change. All existing layouts, components, colors, spacing, and implementations remain exactly as they are.

